smart_taxes_compute_tax_rate = {
	value = smart_taxes_estate_target_satisfaction
	add = smart_taxes_estate_tax_percentage
	subtract = {
		value = smart_taxes_estate_satisfaction
		add = scope:country.modifier:global_estate_satisfaction_recovery
		if = { limit = { scope:estate_type = estate_type:nobles_estate }
			add = scope:country.modifier:nobles_estate_satisfaction_recovery
			max = scope:country.var:nobles_estate_target
		} else_if = { limit = { scope:estate_type = estate_type:clergy_estate }
			add = scope:country.modifier:clergy_estate_satisfaction_recovery
			max = scope:country.var:clergy_estate_target
		} else_if = { limit = { scope:estate_type = estate_type:burghers_estate }
			add = scope:country.modifier:burghers_estate_satisfaction_recovery
			max = scope:country.var:burghers_estate_target
		} else_if = { limit = { scope:estate_type = estate_type:peasants_estate }
			add = scope:country.modifier:peasants_estate_satisfaction_recovery
			max = scope:country.var:peasants_estate_target
		} else_if = { limit = { scope:estate_type = estate_type:tribes_estate }
			add = scope:country.modifier:tribes_estate_satisfaction_recovery
			max = scope:country.var:tribes_estate_target
		} else_if = { limit = { scope:estate_type = estate_type:cossacks_estate }
			add = scope:country.modifier:cossacks_estate_satisfaction_recovery
			max = scope:country.var:cossacks_estate_target
		} else_if = { limit = { scope:estate_type = estate_type:dhimmi_estate }
			add = scope:country.modifier:dhimmi_estate_satisfaction_recovery
			max = scope:country.var:dhimmi_estate_target
		}
	}
	min = smart_taxes_estate_min_tax
	max = smart_taxes_estate_max_tax
}


smart_taxes_estate_max_tax = {
	scope:country = {
		if = { limit = { scope:estate_type = estate_type:nobles_estate }
			value = modifier:nobles_estate_max_tax
		} else_if = { limit = { scope:estate_type = estate_type:clergy_estate }
			value = modifier:clergy_estate_max_tax
		} else_if = { limit = { scope:estate_type = estate_type:burghers_estate }
			value = modifier:burghers_estate_max_tax
		} else_if = { limit = { scope:estate_type = estate_type:peasants_estate }
			value = modifier:peasants_estate_max_tax
		} else_if = { limit = { scope:estate_type = estate_type:tribes_estate }
			value = modifier:tribes_estate_max_tax
		} else_if = { limit = { scope:estate_type = estate_type:cossacks_estate }
			value = modifier:cossacks_estate_max_tax
		} else_if = { limit = { scope:estate_type = estate_type:dhimmi_estate }
			value = modifier:dhimmi_estate_max_tax
		}
		add = modifier:global_estate_max_tax
		min = 0
		max = 1
	}
}

smart_taxes_estate_min_tax = {
	scope:country = {
		if = { limit = { scope:estate_type = estate_type:nobles_estate }
			value = modifier:nobles_estate_min_tax
		} else_if = { limit = { scope:estate_type = estate_type:clergy_estate }
			value = modifier:clergy_estate_min_tax
		} else_if = { limit = { scope:estate_type = estate_type:burghers_estate }
			value = modifier:burghers_estate_min_tax
		} else_if = { limit = { scope:estate_type = estate_type:peasants_estate }
			value = modifier:peasants_estate_min_tax
		} else_if = { limit = { scope:estate_type = estate_type:tribes_estate }
			value = modifier:tribes_estate_min_tax
		} else_if = { limit = { scope:estate_type = estate_type:cossacks_estate }
			value = modifier:cossacks_estate_min_tax
		} else_if = { limit = { scope:estate_type = estate_type:dhimmi_estate }
			value = modifier:dhimmi_estate_min_tax
		}
		add = modifier:global_estate_min_tax
		min = 0
		max = 1
	}
}

smart_taxes_estate_tax_percentage = {
	scope:country = {
		if = { limit = { scope:estate_type = estate_type:nobles_estate }
			value = estate_tax_percentage:nobles_estate
		} else_if = { limit = { scope:estate_type = estate_type:clergy_estate }
			value = estate_tax_percentage:clergy_estate
		} else_if = { limit = { scope:estate_type = estate_type:burghers_estate }
			value = estate_tax_percentage:burghers_estate
		} else_if = { limit = { scope:estate_type = estate_type:peasants_estate }
			value = estate_tax_percentage:peasants_estate
		} else_if = { limit = { scope:estate_type = estate_type:tribes_estate }
			value = estate_tax_percentage:tribes_estate
		} else_if = { limit = { scope:estate_type = estate_type:cossacks_estate }
			value = estate_tax_percentage:cossacks_estate
		} else_if = { limit = { scope:estate_type = estate_type:dhimmi_estate }
			value = estate_tax_percentage:dhimmi_estate
		}
	}
}

smart_taxes_estate_target_satisfaction = {
	scope:country = {
		if = { limit = { scope:estate_type = estate_type:nobles_estate }
			value = estate_target_satisfaction:nobles_estate
		} else_if = { limit = { scope:estate_type = estate_type:clergy_estate }
			value = estate_target_satisfaction:clergy_estate
		} else_if = { limit = { scope:estate_type = estate_type:burghers_estate }
			value = estate_target_satisfaction:burghers_estate
		} else_if = { limit = { scope:estate_type = estate_type:peasants_estate }
			value = estate_target_satisfaction:peasants_estate
		} else_if = { limit = { scope:estate_type = estate_type:tribes_estate }
			value = estate_target_satisfaction:tribes_estate
		} else_if = { limit = { scope:estate_type = estate_type:cossacks_estate }
			value = estate_target_satisfaction:cossacks_estate
		} else_if = { limit = { scope:estate_type = estate_type:dhimmi_estate }
			value = estate_target_satisfaction:dhimmi_estate
		}
	}
}

smart_taxes_estate_satisfaction = {
	scope:country = {
		if = { limit = { scope:estate_type = estate_type:nobles_estate }
			value = estate_satisfaction:nobles_estate
		} else_if = { limit = { scope:estate_type = estate_type:clergy_estate }
			value = estate_satisfaction:clergy_estate
		} else_if = { limit = { scope:estate_type = estate_type:burghers_estate }
			value = estate_satisfaction:burghers_estate
		} else_if = { limit = { scope:estate_type = estate_type:peasants_estate }
			value = estate_satisfaction:peasants_estate
		} else_if = { limit = { scope:estate_type = estate_type:tribes_estate }
			value = estate_satisfaction:tribes_estate
		} else_if = { limit = { scope:estate_type = estate_type:cossacks_estate }
			value = estate_satisfaction:cossacks_estate
		} else_if = { limit = { scope:estate_type = estate_type:dhimmi_estate }
			value = estate_satisfaction:dhimmi_estate
		}
	}
}
