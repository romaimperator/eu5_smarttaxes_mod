# Off-screen driver that applies smart tax rates when the recalculate flag is set.
widget = {
	name = "smart_taxes_hidden_window"
	widgetid = smart_taxes_hidden_window
	layer = top
	visible = "[EqualTo_CFixedPoint('(CFixedPoint)0', '(CFixedPoint)0')]"
	position = { -10000 1 }
	widget = {
		visible = "[GetPlayer.Exists]"
		# Rebuilds and re-fires _show each time the recalculate flag is set; tears down when it clears.
		widget = {
			size = { 0 0 }
			visible = "[And(GetPlayer.Exists, GetScriptedGui('smart_taxes_should_recalculate').IsShown(GuiScope.SetRoot(GetPlayer.MakeScope).End))]"
			state = {
				name = _show
				duration = 0.5
				on_finish = "[GetScriptedGui('smart_taxes_clear_recalculate').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
			}
			datacontext = "[GetEconomyView]"
			datamodel = "[EconomyView.GetTaxRateSettings]"
			item = {
				widget = {
					overlappingitembox = {
						size = { 0 0 }
						visible = "[And(TaxRateSetting.IsUsingAutoTaxRate, GetScriptedGui('smart_taxes_enabled').IsShown(GuiScope.SetRoot(TaxRateSetting.GetEstate.GetCountry.MakeScope).AddScope('estate_type', TaxRateSetting.GetEstate.GetType.MakeScope).End))]"
						state = {
							name = _show
							trigger_on_create = yes
							on_start = "[TaxRateSetting.ToggleAutoTaxRate]"
						}
					}
					smart_taxes_estate_tax_slider = {}
				}
			}
		}
	}
}
